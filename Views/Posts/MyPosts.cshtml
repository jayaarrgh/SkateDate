
@foreach (var post in Model)
{
    <div>
        <h4>@post.Author</h4>
        <p>@post.Message</p>
        <p>@post.CreatedDate</p>
        <p><a asp-controller="Posts" asp-action="Post" asp-route-PostID="@post.PostID">Click to Edit or Remove This Post</a></p>
    </div>
    <hr />
}